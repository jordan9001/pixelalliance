Order of todo:
	Client Optimizations:
		Only send if cell changed
		have loading message until current block is loaded, don't allow drawing until it is loaded

	Server Optimizations:
		Only send if cell changed
		send with as big of frame groups as possible
		Add Block message type and send it for players entering an adjacent block
		Don't send updates outside of current player block


Other:
	top
	locked
	Good scrolling
	Chat
	Better controls for collisions/top/layers/colors
